/**
 * Relic templates.
 *
 * These moved to `thetowersdk` when game data was extracted out of
 * `@tmrxjd/platform`; importing them from the old home silently stopped
 * resolving once no published platform version carried them.
 */
export { RELIC_TEMPLATES, type RelicTemplate } from 'thetowersdk/data'
export { RELIC_TEMPLATES as default } from 'thetowersdk/data'
